# CLAUDE.md — coworking-spaces

This dataset contains information about coworking spaces in different locations in the world.

Currently, there is data about Bali and the country of Georgia. The data for Bali and Georgia are currently still split into separate files but are intended to be combined in a singel file eventually.

## Giraffe computed fields

Fields that are omitted from the data files but are intended to be added in as computed fields in Giraffe:

- `google_maps_*`  # TODO: determine exact set
